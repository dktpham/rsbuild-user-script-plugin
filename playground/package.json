{
  "name": "playground",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "npx rsbuild dev",
    "build": "npx rsbuild build",
    "preview": "npx rsbuild preview"
  }
}
